export namespace Enum {
    export enum WidthType {
        ABSOLUTE = 'ABSOLUTE',
        RELATIVE = 'RELATIVE'
    }
    export enum DataTye {
        BUTTON = 'BUTTON',
        STRING = 'STRING'
    }
}
